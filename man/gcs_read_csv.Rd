% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gcs.R
\name{gcs_read_csv}
\alias{gcs_read_csv}
\title{Read a CSV file from Google Cloud Storage}
\usage{
gcs_read_csv(bucket, object, clean_names = TRUE)
}
\arguments{
\item{bucket}{Character string. The name of the GCS bucket containing the file.}

\item{object}{Character string. The path to the CSV file within the bucket.}

\item{clean_names}{Logical. If \code{TRUE} (default), column names are cleaned
using \code{\link[janitor:clean_names]{janitor::clean_names()}}. If \code{FALSE}, original column names are preserved.}
}
\value{
A data frame (tibble) containing the CSV data.
}
\description{
Reads a CSV file from Google Cloud Storage into a data frame using the
arrow package for efficient reading. Optionally cleans column names using
janitor::clean_names().
}
\details{
This function uses arrow's CSV reader which is optimized for performance
and can handle large files efficiently. The GCS path is constructed using
glue for safe string interpolation.

By default, column names are cleaned using janitor::clean_names() to convert
them to snake_case and remove special characters. Set \code{clean_names = FALSE}
to preserve original column names.
}
\examples{
\dontrun{
# Read a CSV and clean column names
data <- gcs_read_csv("my-project-data", "raw/customers.csv")

# Read a CSV preserving original column names
data <- gcs_read_csv("my-project-data", "raw/customers.csv", clean_names = FALSE)

# Use with gcs_auth_bucket for authenticated reading
gcs_auth_bucket("my-project-data")
data <- gcs_read_csv("my-project-data", "processed/sales_2024.csv")
}

}
\seealso{
\code{\link[arrow:read_delim_arrow]{arrow::read_csv_arrow()}} for reading options,
\code{\link[janitor:clean_names]{janitor::clean_names()}} for name cleaning details
}
