% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gcs.R
\name{tar_gcs_csv}
\alias{tar_gcs_csv}
\title{Create a targets pipeline target for writing CSVs to GCS}
\usage{
tar_gcs_csv(name, data, bucket, object, ...)
}
\arguments{
\item{name}{Symbol. The name of the target (unquoted).}

\item{data}{Expression. The data frame to write to GCS. Can reference upstream
targets or other R objects.}

\item{bucket}{Character string. The name of the GCS bucket to write to.}

\item{object}{Character string. The destination path for the CSV file within
the bucket.}

\item{...}{Additional arguments passed to \code{\link[targets:tar_target]{targets::tar_target()}}.}
}
\value{
A targets target object suitable for use in a \verb{_targets.R} file.
}
\description{
Creates a targets pipeline target that writes a data frame to Google Cloud
Storage as a CSV file. This is a convenience wrapper around targets::tar_target()
specifically designed for GCS CSV outputs.
}
\details{
This function creates a targets target that:
\enumerate{
\item Authenticates with GCS using \code{gcs_auth_bucket()}
\item Writes the data to GCS using \code{gcs_write_csv()} with metadata enabled
\item Returns the metadata as the target value
}

The target is created with format = "qs" for efficient serialization of
the metadata list.

The \code{data} parameter is captured as an expression and evaluated at build time,
allowing you to reference upstream targets or other R objects.
}
\examples{
\dontrun{
# In your _targets.R file:
library(targets)
library(ojoutils)

tar_plan(
  # Process some data
  tar_target(raw_data, read_csv("input.csv")),
  tar_target(clean_data, clean_my_data(raw_data)),

  # Write to GCS as a target
  tar_gcs_csv(
    gcs_output,
    clean_data,
    bucket = "my-project-data",
    object = "processed/clean_data.csv"
  )
)

# With additional tar_target options
tar_gcs_csv(
  gcs_output,
  processed_data,
  bucket = "my-project-data",
  object = "output/results.csv",
  priority = 1
)
}

}
\seealso{
\code{\link[targets:tar_target]{targets::tar_target()}} for general target options,
\code{\link[=gcs_write_csv]{gcs_write_csv()}} for the underlying write operation
}
