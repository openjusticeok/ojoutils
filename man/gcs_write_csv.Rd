% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gcs.R
\name{gcs_write_csv}
\alias{gcs_write_csv}
\title{Write a CSV file to Google Cloud Storage}
\usage{
gcs_write_csv(data, bucket, object, meta = FALSE)
}
\arguments{
\item{data}{A data frame to write to GCS.}

\item{bucket}{Character string. The name of the GCS bucket to write to.}

\item{object}{Character string. The destination path for the CSV file within
the bucket (e.g., "folder/subfolder/filename.csv").}

\item{meta}{Logical. If \code{TRUE}, returns a list with object metadata. If \code{FALSE}
(default), returns the GCS path invisibly.}
}
\value{
If \code{meta = TRUE}, a list containing:
\itemize{
\item \code{path}: The full GCS path to the object
\item \code{md5_hash}: The MD5 hash of the uploaded object
\item \code{generation}: The object's generation number
\item \code{size}: The object size in bytes
\item \code{updated}: The last update timestamp
}

If \code{meta = FALSE}, the GCS path (invisibly).
}
\description{
Writes a data frame to a CSV file in Google Cloud Storage using the arrow
package. Optionally returns metadata about the uploaded object.
}
\details{
This function uses arrow's CSV writer for efficient writing of data frames
to GCS. The file is written directly to the specified GCS path without
requiring temporary local storage.

When \code{meta = TRUE}, the function retrieves and returns metadata about the
uploaded object including:
\itemize{
\item The GCS path
\item MD5 hash for integrity verification
\item Generation number for versioning
\item File size
\item Last updated timestamp
}
}
\examples{
\dontrun{
# Write a data frame to GCS
gcs_write_csv(mtcars, "my-project-data", "output/mtcars.csv")

# Write and get metadata back
meta <- gcs_write_csv(mtcars, "my-project-data", "output/mtcars.csv", meta = TRUE)
print(meta$md5_hash)

# Use with authentication
gcs_auth_bucket("my-project-data")
gcs_write_csv(my_data, "my-project-data", "processed/results.csv")
}

}
\seealso{
\code{\link[arrow:write_csv_arrow]{arrow::write_csv_arrow()}} for write options,
\code{\link[googleCloudStorageR:gcs_get_object]{googleCloudStorageR::gcs_get_object()}} for metadata retrieval
}
